apiVersion: v1
kind: Service
metadata:
  name: retail-api-service
  namespace: sa-retail
spec:
  selector:
    app: retail-api
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
